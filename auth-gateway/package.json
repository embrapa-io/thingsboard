{
  "name": "thingsboard-auth-gateway",
  "private": true,
  "version": "0.1.0",
  "description": "Internal ThingsBoard login gate requiring the server-scoped manager attribute.",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
